---
title: FeatureCard
description: Documentation for the FeatureCard component
---

import { FeatureCard } from '@/components/FeatureCard'
import { Canvas, Meta, Story, Controls, ArgTypes } from 'storybook/blocks'
import * as FeatureCardStories from './FeatureCard.stories'

<Meta of={FeatureCardStories} />

# FeatureCard

The `FeatureCard` component is a reusable UI element.

## Import

```tsx
import { FeatureCard } from '@/components/FeatureCard'
```

## Usage

### Basic Example

<Canvas of={FeatureCardStories.Default} />

```tsx
<FeatureCard>
  Content
</FeatureCard>
```

## Props

<ArgTypes of={FeatureCard} />

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `icon` | `ReactNode` | `-` | - |
| `title` | `string` | `-` | - |
| `description` | `string` | `-` | - |
| `accentColor` | `string` | `-` | - |


## Accessibility

The `FeatureCard` component follows WAI-ARIA guidelines:

- Supports keyboard navigation
- Includes proper ARIA attributes
- Compatible with screen readers

### Keyboard Interactions

| Key | Description |
|-----|-------------|
| `Tab` | Moves focus to the component |
| `Enter` | Activates the component |
| `Space` | Activates the component |

## Composition

### With Other Components

```tsx
import { Stack } from '@chakra-ui/react'

<Stack spacing={4}>
  <FeatureCard>
    First item
  </FeatureCard>
  <FeatureCard>
    Second item
  </FeatureCard>
</Stack>
```

## Responsive Design

Use responsive props for different screen sizes:

```tsx
<FeatureCard
  
  width={{ base: '100%', md: 'auto' }}
>
  Responsive FeatureCard
</FeatureCard>
```

## Best Practices

1. **Accessibility**: Always provide meaningful content or aria-label
4. **Composition**: Combine with other components using Stack or Flex for layouts
5. **Performance**: Avoid creating new callback functions on each render

## Related Components

- [Box](/components/box) - Base layout component
- [Stack](/components/stack) - Layout with spacing
- [Flex](/components/flex) - Flexbox layout
